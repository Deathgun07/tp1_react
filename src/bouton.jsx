export function IncrementBtn({onClick}) {
    return <button onClick={onClick} >Incrementer</button>
  }
  
export function DecrementBtn({onClick}) {
    return <button onClick={onClick}>Decrementer</button> 
  }